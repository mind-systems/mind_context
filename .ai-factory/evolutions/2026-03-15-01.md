# Evolution: 2026-03-15

## Intelligence Summary

- Patches analyzed: 0 (pre-patch era — commit history used instead)
- Git history analyzed: 13 commits (mind_api, dd01377f→HEAD) + 5 commits (mind_mobile, 436f2017→HEAD)
- Tech stack: NestJS 11 + TypeORM + PostgreSQL + Socket.io + Winston + Jest (API) / Flutter + Riverpod + Drift (Mobile)

## Recurring Patterns Identified

### mind_api
- `process.env` direct access in services → migrated to ConfigService (1 fix, systemic risk)
- Incomplete lifecycle interfaces: `OnApplicationBootstrap` without `OnApplicationShutdown` (1 fix)
- Missing test fixture fields after interface changes (systemic — all gateway specs touched)
- Hardcoded WS constants → ConfigService (4 constants moved in one cleanup commit)
- Formatting pass always follows feature implementation (every feature)
- Unsafe type casts removed (1 fix)
- Ambiguous field names in socket emissions (1 rename)

### mind_mobile
- `StreamSubscription` not cancelled in `dispose()` (1 fix, memory leak class)
- Flutter imports leaking into domain layer — PresenceNotifier rewrote (1 fix)
- Missing switch cases for socket status values — state stuck (1 fix)
- Logic triggered too early in lifecycle (1 fix)
- Testability blocked by concrete Notifier dependencies (1 TODO added)

## Improvements Applied

### aif-review (restructured as dispatcher)
- Rewritten as a short 3-step dispatcher: get diff → detect scope → delegate
- Auto-routes to `aif-review-api` or `aif-review-mobile` based on changed file paths
- Eliminates need for user to remember which sub-skill to call

### aif-review-api (NEW skill)
- Created from scratch with full NestJS-specific checklist
- Includes all generic checks (correctness, security, performance, testing)
- Added: NestJS Conventions section (ConfigService, lifecycle interfaces, no unsafe casts)
- Added: WebSocket (RealtimeModule) section (lifecycle, hardcoded constants, fixture completeness, error format, Logger)
- Added: TypeORM & Migrations section (CLI migration generation, column types, relations)
- Driven by: commit 9ea39aa (ConfigService migration), commit e80ad52 (fixture updates, liveSessionId rename)

### aif-review-mobile (NEW skill)
- Created from scratch with full Flutter/Dart-specific checklist
- Includes all generic checks (correctness, performance, testing)
- Added: Flutter/Dart section (StreamSubscription cancel, no flutter imports in domain, sealed class completeness, lifecycle hooks, App.dart order, injectable streams)
- Added: Architecture section (Riverpod + domain layer rules, Drift migrations)
- Added: API Sync section (Dart model sync, AuthInterceptor, Dio client)
- Driven by: review-fixes commit (cancel leak, WidgetsBindingObserver removal, idle status case)

### aif-fix
- Added NestJS-specific logging rule: `new Logger(ClassName.name)` instead of `console.log`
- Explains why: Winston pipeline bypassed by console.log, logs won't appear in rotation files
- Added NestJS code example alongside the existing generic pattern
- Driven by: project uses Winston with daily rotation; console.log in fixes is a dead end for debugging

## Architecture Decision

User requested and we implemented: dispatcher pattern for `aif-review`. The main skill is now a 30-line router. Project-specific intelligence lives in dedicated sub-skills. This mirrors the existing `aif-plan-mind-api` / `aif-plan-mind-mobile` pattern.
